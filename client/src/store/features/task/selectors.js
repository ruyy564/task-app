export const selectTaskStatus = (state) => state.task.status;

export const selectTasks = (state) => state.task.tasks;

export const selectTaskCount = (state) => state.task.count;

export const selectTaskErrorMessage = (state) => state.task.errorMessage;

export const selectTaskErrors = (state) => state.task.errors;
